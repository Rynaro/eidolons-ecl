{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Rynaro/eidolons-ecl/blob/v2.0.0/schemas/per-eidolon/repair-failed-report.v1.json",
  "title": "ECL repair-failed-report profile v1",
  "description": "APIVR-Δ emit on the 3-failure threshold; consumed by VIGIL.",
  "allOf": [
    { "$ref": "_base-profile.v1.json" },
    {
      "type": "object",
      "required": ["failure_category", "attempts", "last_test_command"],
      "properties": {
        "kind":    { "const": "repair-failed-report" },
        "eidolon": { "const": "apivr" },
        "failure_category": {
          "type": "string",
          "description": "APIVR-Δ failure taxonomy slug (e.g. 'flaky-test', 'env-mismatch')."
        },
        "attempts": {
          "type": "integer",
          "minimum": 3,
          "description": "MUST be >= 3 to trigger escalation per APIVR-Δ P0."
        },
        "last_test_command": { "type": "string" },
        "trace_artifact_paths": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    }
  ]
}
