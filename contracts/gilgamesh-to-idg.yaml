contract_version: "1.0"
from: gilgamesh
to: idg
edge_origin: emitted-request
performatives_allowed:
  - PROPOSE
  - INFORM
  - ESCALATE
artifacts:
  - kind: handoff-request
    schema_ref: ../schemas/per-eidolon/handoff-request.v1.json
    evidence_anchor_required: false
context_delta:
  token_budget_max: 3000
  required_handles: []
trust_level: standard
notes: >
  Gilgamesh PROPOSEs a documentation request upward, suggested_specialist:
  idg, when a completed or in-flight mission needs a chronicle, ADR, runbook,
  or change-narrative that exceeds Gilgamesh's own no-permanent-memory,
  deliberately-boring-worker posture (HC8). This is a request the orchestrator
  routes, not a dispatch — Gilgamesh has no downstream handoffs of its own
  (roster: gilgamesh.handoffs.downstream: [], R-007/HC8). edge_origin:
  emitted-request (not `roster`) reconciles that empty downstream array with
  this outbound contract's existence (R-050/AC-F05; CRIT-009).
